# Booking Progress Widget

Owns the booking cycle progress strip and editable progress-step rendering used by the main chat page.

Use `demo.html` in this folder to preview the live widget in isolation.
