# Slot List Widget

Owns the reusable list of clickable slot actions used by the scheduling sandbox and future chat-driven scheduling UI.

Use `demo.html` in this folder to preview the live widget in isolation.
